ALGORITMO DecimalParaBinario;
{
    inteiro decimal, resto, peso, binario_final, binario_invertido;
    escreva("Digite um número inteiro não negativo:");
    leia(decimal);
    binario_final = 0;
    peso = 1; 

    ENQUANTO (decimal > 0) {
        resto = decimal % 2;
        binario = binario + (resto * peso);
        decimal = decimal / 2;
         peso = peso * 10;
    }
	
    escreva("O número em binário é: ", binario_invertido);
}s